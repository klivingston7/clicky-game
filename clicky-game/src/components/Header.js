import React from 'react';

const Header = props => (
    <div className="container text-center" id="header-container">
        <h1>Clicky Game</h1>
        <p>How to play:</p>
        <p>Click on an image to get started! After each click, the images will shuffle. The goal is to select each image once!</p>
    </div>
)

export default Header;